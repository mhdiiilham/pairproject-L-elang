const category = require('./categoryController')
const item = require('./itemController')
const user = require('./userController')
const userItem = require('./userItemController')
const body = require('./bodyController')

module.exports = { category, item, user, userItem, body }